# Copy to .env in project root (same folder as manage.py)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash-lite

# Real-time chat (optional — uses in-memory layer if not set)
# REDIS_URL=redis://127.0.0.1:6379/0
